import json

def lambda_handler(event, context):
    # TODO implement
    print('This is sample lambda function')
    if event['Test'] == 'Earth':
        return 'Moon'
    elif event['Test'] == 'Sun':
        return 'THis is not planet'
    else :
        return 'This is not recongnizible'
 
